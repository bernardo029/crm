import { Check, Plus } from 'lucide-react'
import type { Task } from '../lib/types'
export function Tasks({tasks,onAdd,onToggle}:{tasks:Task[];onAdd:()=>void;onToggle:(t:Task)=>void}){return <><div className="page-heading"><div><span className="eyebrow">Atividades</span><h1>Tarefas</h1><p>Follow-ups e próximos passos da equipe.</p></div><button className="primary" onClick={onAdd}><Plus size={17}/>Nova tarefa</button></div><section className="panel task-list">{tasks.map(t=><div className={t.status==='done'?'task done':'task'} key={t.id}><button className="check-btn" onClick={()=>onToggle(t)}>{t.status==='done'?<Check size={16}/>:null}</button><div className="task-main"><strong>{t.title}</strong><span>{t.due_at?new Date(t.due_at).toLocaleString('pt-BR'):'Sem vencimento'}</span></div><span className={`priority ${t.priority}`}>{t.priority}</span></div>)}{tasks.length===0&&<div className="empty">Nenhuma tarefa cadastrada.</div>}</section></>}
